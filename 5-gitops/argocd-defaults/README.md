argocd 를 편하게 운용 관리하려면 나에게 두가지가 필요하다.

1. 각 노드에 이미지 옮겨다 세팅할 방법
-> 이미지 빌드 및 생성을 호스트에서 할수있으나 비권장하고 싶으니 게스트 (마스터 클러슽터에서 진행하여) (워커노드쪽에 복사이동시키자) (소스가 비권장또는 별로라면 개선. 앤서블로 동작시키면될듯한데. 고민)

2. argocd Argocd
 Settings Repositories 
 connect repo (github) 사용 도 자동화 시키고싶다. (PAT 제공해주면 되지 ㅇ않나)

 3. 넥서스(이미지관리)는 당분간 생략하고 필요시 깃소스(다른 깃레포지토리)를 내려받고 도커파일은 해당 프로젝트 소스에 만들어서 그 내용바탕으로 이미지 생성하도록 build 체계 생성 (jenkins 와 같은 빌드도구 도 생략 당분간)

 4. 지금구조면 무작위 mcp 파이선 서버에 대해서 argocd 배포 까지 완성할수있을거 같은데.

 5. 일단 아래 소스 기준으면 될거같다 (gamin auth 인증 관련은 나중에 소스에서 해결)

 https://github.com/Jeongjuwon-259/hermes-gamin-connect-mcp.git