---
# 5-gitops/ansible/kubeconfig-setup-playbook.yml
# 마스터 노드(node-1, control_plane)에 kubeconfig 셋업
#
# 실행:
#   cd 5-gitops/ansible
#   ansible-playbook kubeconfig-setup-playbook.yml

- name: Setup kubeconfig on control plane node
  hosts: control_plane
  become: yes
  roles:
    - kubeconfig-setup